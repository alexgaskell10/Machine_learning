from examples_scripts import *
from random import seed

if __name__ == '__main__':
    # example_two_players_users()
    # example_user_manual_board_vs_full_automatic()
    # example_user_automatic_board_vs_full_automatic()
    # player_automatic_vs_player_random()
    player_automatic_vs_player_automatic()


    ## Delete below here before submitting ##
    # i = 9000
    # for i in range(i):
    #     seed(i)
    #     print(f'random seed = {i}')
    #     board = BoardAutomatic()
    #     print(f'iteration i: {i}')

    # winners = []
    # x = 500
    # for i in range(x):
    #     # seed(i)
    #     print(f'random seed = {i+1}')
    #     winners.append(player_automatic_vs_player_automatic())
    # print(f'Alice won: {winners.count("Alice")} / {i+1} games')
